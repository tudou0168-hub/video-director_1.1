# 07 Scene Recipes

The executable JSON recipes live in `recipes/`.

Required:

- `ai_content_system_60s.json`
- `agent_tool_tutorial_60s.json`
- `knowledge_explainer_60s.json`
- `product_proof_60s.json`

All recipe segments must include:

`start`, `end`, `template`, `components`, `primaryText`, `subtitleCN`, `colorTheme`, `safeZone`, `motion`, `snapshotAt`.
